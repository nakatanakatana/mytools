apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
spec:
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: litestream-controller
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
